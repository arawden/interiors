alias vim="nvim"

alias pacaur="pikaur"
alias update="pikaur -Syu"

alias tar-gz="tar -xzvf"

alias sound=pavucontrol
alias wifi=wicd-gtk

alias pyserver="python -m http.server 8000 &>/dev/null"
alias sprunge="curl -F 'sprunge=<-' http://sprunge.us"

alias rs="sh /home/avr/downloads/programs/rs.sh"

# Git aliases
alias gst="git status"
alias gd="git diff"


# Force xterm on ssh
alias ssh="TERM=xterm-256color ssh"

# Aliases that take arguments

# Adjust brightness on laptop screen
# brightness () {
#   xrandr --output LVDS-1 --brightness "$1"
# }
